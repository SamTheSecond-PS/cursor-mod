[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cursorx"
version = "2.0.1"
description = "Cursor-based text manipulation engine"
authors = [
  { name = "Sarvesh ER" }
]
readme = "README.md"
requires-python = ">=3.7"